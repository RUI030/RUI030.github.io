$(document).ready(
    function () {
        //Button click events
        $('.to-about').on('click', function () { scrollTo('#about') });
        $('.to-pages').on('click', function () { scrollTo('#pages') });
        $('.to-links').on('click', function () { scrollTo('#links') });
    }
);